<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="content-type" content="text/html; charset=utf-8"></meta>
        <title>{{ page.title }}</title>
        {% if not page.inline_css %}
            <link rel="stylesheet" href="{{ page.css_url }}" /> 
        {% endif %}
    </head>
    {% include 'body.twig' %}
</html>